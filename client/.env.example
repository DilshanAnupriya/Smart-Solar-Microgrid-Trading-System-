# Copy this file to .env before running the client.
# .env itself is git-ignored, so each developer sets their own API address.

# Base URL of the Smart Solar Web API.
# The web application is a UI layer only: every operation goes through this API.
VITE_API_BASE_URL=http://localhost:5262/api
